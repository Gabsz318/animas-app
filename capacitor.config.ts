import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'animas.bosque.app',
  appName: 'animas-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
